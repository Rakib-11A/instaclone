<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid"> <!-- Added container-fluid for proper spacing -->
     <%= link_to "Home", root_path, class: "navbar-brand", style: "margin-left: 20px" %>
     <% if user_signed_in? && current_user.avatar.attached? %>
         <%= image_tag(current_user.avatar, style: "width: 50px; height: 50px; border-radius: 50%;") %>
      <% end %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent" style="margin-left: 20px">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0"> <!-- Changed mr-auto to me-auto -->
        <li class="nav-item">
          <%= link_to "Upload Post", new_post_path, class: "btn btn-outline-primary", remote: true %>
        </li>
        <li class="nav-item dropdown">
          <%= link_to "About", home_about_path, class: "btn btn-outline-secondary"%>
        </li>

        <li class="nav-item dropdown">
          <%= link_to "My Posts", posts_myposts_path, class: "btn btn-outline-success"%>
        </li>
        <li class="nav-item dropdown">
          <%= link_to "Edit Account", edit_user_registration_path, class: "btn btn-outline-danger" %>
        </li>
        <% if user_signed_in? %>

          <li class="nav-item dropdown">
            <%= link_to "View Profile", current_user, class: "btn btn-outline-warning"%>
          </li>

          <li class="nav-item dropdown">
            <%= link_to "Sing Out", destroy_user_session_path, class: "nav-link btn btn-danger"%>
          </li>
        <% else %>
            <li class="nav-item dropdown">
            <%= link_to "Sing Up", new_user_registration_path, class: "nav-link btn btn-danger"%>
        </li>
        <% end %>
      </ul>

      <!-- Corrected Bootstrap 5 Search Form -->
      <%# <form class="d-flex" role="search">
        <input class="form-control me-2" type="search" placeholder="Search">
        <button class="btn btn-outline-success" type="submit">Search</button>
      </form> %>
    </div>
  </div>
</nav>